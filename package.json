{
  "name": "lui-explore",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "dev:all": "concurrently -c bgMagenta,bgBlue -n SCSS,SERVER --kill-others \"npm:sass\" \"npm:dev\"",
    "sass": "sass --style=compressed --no-source-map --watch src/styles/scss:src/styles",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "concurrently": "^7.1.0",
    "sass": "^1.50.0",
    "vite": "^2.9.0"
  }
}